#ifndef EXCHANGE_HPP
#define EXCHANGE_HPP

//if (!czy_stala_wymiana)
//{
//    cfg.load_settings(&cfg_params, &com_params, configFile);
//    static const QString path = cfg_params.dbfile;
//    db = new dbmanager(path);
//    int tmp_excg = db->getlastSentExchange();
//    qDebug() << "DO ZWIEKSZENIA WYMIANA " << tmp_excg;
//    return tmp_excg + 1;
//}
//pobiera ostatnia wyslana wymiane
//jesli stala to dopisuje taka sama
//jesli nie stala to zwieksza o 1

bool if_incremental()
#endif // EXCHANGE_HPP
